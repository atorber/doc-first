# Node.js 20 安装脚本使用指南

## 📦 脚本说明

提供了两个安装脚本，适用于 macOS 系统：

### 1. `install-nodejs20.sh` - 交互式安装（推荐）

**特点**：
- ✅ 交互式选择安装方式（Homebrew 或 nvm）
- ✅ 自动检测已安装的 Node.js
- ✅ 自动安装 Homebrew（如需要）
- ✅ 详细的安装过程提示

**使用方法**：
```bash
./install-nodejs20.sh
```

### 2. `install-nodejs20-auto.sh` - 全自动安装

**特点**：
- ✅ 完全自动化，无需交互
- ✅ 自动检测并安装依赖
- ✅ 适合 CI/CD 或批量部署

**使用方法**：
```bash
./install-nodejs20-auto.sh
```

## 🚀 快速开始

### 方法 1: 直接运行（推荐）

```bash
# 下载脚本
curl -O https://raw.githubusercontent.com/atorber/doc-first/main/scripts/install-nodejs20.sh

# 添加执行权限
chmod +x install-nodejs20.sh

# 运行安装
./install-nodejs20.sh
```

### 方法 2: 克隆仓库后运行

```bash
cd doc-first
./scripts/install-nodejs20.sh
```

## 📋 安装要求

- macOS 系统
- 网络连接（用于下载 Homebrew 和 Node.js）
- 管理员权限（安装 Homebrew 时需要）

## 🔧 安装方式说明

### Homebrew（推荐）

**优点**：
- macOS 上最常用的包管理器
- 安装简单，管理方便
- 自动处理依赖关系

**安装位置**：
- Intel Mac: `/usr/local/bin/node`
- Apple Silicon: `/opt/homebrew/bin/node`

### nvm（Node Version Manager）

**优点**：
- 可以管理多个 Node.js 版本
- 方便切换版本
- 适合需要多版本开发的场景

**安装位置**：
- `~/.nvm/versions/node/v20.x.x/`

## ✅ 验证安装

安装完成后，运行以下命令验证：

```bash
# 检查 Node.js 版本
node -v
# 应该显示: v20.x.x

# 检查 npm 版本
npm -v

# 检查安装路径
which node
```

## 🐛 常见问题

### Q: 提示 "Permission denied"
**A**: 需要添加执行权限：
```bash
chmod +x install-nodejs20.sh
```

### Q: Homebrew 安装失败
**A**: 检查网络连接，或手动安装 Homebrew：
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Q: 安装后 node 命令找不到
**A**: 重新加载 shell 配置：
```bash
source ~/.zshrc
# 或
source ~/.bash_profile
```

### Q: 想卸载 Node.js
**A**: 
- 如果使用 Homebrew: `brew uninstall node@20`
- 如果使用 nvm: `nvm uninstall 20`

## 📝 注意事项

1. **Apple Silicon Mac**: 脚本会自动处理 M1/M2 Mac 的路径问题
2. **已安装旧版本**: 脚本会检测并提示是否需要升级
3. **权限问题**: 安装 Homebrew 时需要输入管理员密码

## 🎯 推荐使用场景

- **开发环境**: 使用 `install-nodejs20.sh`（交互式，更灵活）
- **CI/CD**: 使用 `install-nodejs20-auto.sh`（自动化，无需交互）
- **团队统一环境**: 使用 `install-nodejs20-auto.sh`（确保一致性）

---

*如有问题，请查看脚本源码或提交 Issue*
